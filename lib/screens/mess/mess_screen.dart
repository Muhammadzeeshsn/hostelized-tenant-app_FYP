import 'package:flutter/material.dart';

class MessScreen extends StatelessWidget {
  const MessScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Mess')));
}
